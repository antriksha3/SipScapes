export default function TimelinePage() {
  return (
    <div style={{ padding: 24, fontFamily: 'var(--body-font)', color: 'var(--ink)' }}>
      <p>TimelinePage — stub, full implementation coming in the next step.</p>
    </div>
  );
}
