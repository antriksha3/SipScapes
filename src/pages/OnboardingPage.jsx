export default function OnboardingPage() {
  return (
    <div style={{ padding: 24, fontFamily: 'var(--body-font)', color: 'var(--ink)' }}>
      <p>OnboardingPage — stub, full implementation coming in the next step.</p>
    </div>
  );
}
