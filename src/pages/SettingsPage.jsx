export default function SettingsPage() {
  return (
    <div style={{ padding: 24, fontFamily: 'var(--body-font)', color: 'var(--ink)' }}>
      <p>SettingsPage — stub, full implementation coming in the next step.</p>
    </div>
  );
}
