export default function MoodPage() {
  return (
    <div style={{ padding: 24, fontFamily: 'var(--body-font)', color: 'var(--ink)' }}>
      <p>MoodPage — stub, full implementation coming in the next step.</p>
    </div>
  );
}
