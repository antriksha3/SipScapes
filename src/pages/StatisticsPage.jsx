export default function StatisticsPage() {
  return (
    <div style={{ padding: 24, fontFamily: 'var(--body-font)', color: 'var(--ink)' }}>
      <p>StatisticsPage — stub, full implementation coming in the next step.</p>
    </div>
  );
}
